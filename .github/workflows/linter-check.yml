name: linter-check

on:
  - push
  - pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install
        run: make install
      - name: Run linter
        run: make lint
